{
  "name": "zvidlib-web-canvas-example",
  "private": true,
  "type": "module",
  "scripts": {
    "build:wasm": "wasm-pack build ../.. --target web --out-dir examples/web_canvas/pkg --no-default-features --features web",
    "dev": "pnpm run build:wasm && vite"
  },
  "devDependencies": {
    "vite": "^5.4.11"
  }
}
